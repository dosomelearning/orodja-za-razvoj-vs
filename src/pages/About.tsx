// src/pages/About.tsx

import React from 'react';

const About: React.FC = () => {
    return (
        <div>
            <h2>About</h2>
            <p>This is a demo library app built with React and TypeScript.</p>
        </div>
    );
};

export default About;

